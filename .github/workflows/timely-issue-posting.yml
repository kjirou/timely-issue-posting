on:
  schedule:
    - cron: '45 2 * * *'
  workflow_dispatch:
jobs:
  post-issues:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/post-issue-2
        with:
          template-id: foo
